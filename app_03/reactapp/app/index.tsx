import {View, StyleSheet} from 'react-native';

export default function Index() {
    return (
        <View>
        </View>
    );
}
const estilo = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: 'rrgb(255, 255, 255)',
        alignItems: 'center',
        justifyContent: 'center',
    },
});