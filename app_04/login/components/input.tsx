import { TextInput } from "react-native"

export function Input () {
    return <TextInput />
}
const style = StyleSheet.create({
    input: {                         
        width: '100%',
        height: 48,   
        borderWidth: 1,
        borderColor: '#DCDCDC',
        borderRadius: 8,
        
    }
})